<%- model_class = Usermanual -%>
<div class="page-header">
  <h1><%=t '.title', :default => Usermanual %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:document_name) %></th>
      <th><%= model_class.human_attribute_name(:document_type) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @usermanuals.each do |usermanual| %>
      <tr>
        <td><%= usermanual.id %></td>
        <td><%= usermanual.document_name %></td>
        <td><%= usermanual.document_type %></td>
        <td><%=l usermanual.created_at, format: :long %></td>
        <td>
          <%= link_to usermanual_path(usermanual), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to usermanual_path(usermanual), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_usermanual_path,
            :class => 'btn btn-primary' %>
