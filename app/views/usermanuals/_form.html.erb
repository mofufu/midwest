<%= form_for(@usermanual) do |f| %>
  <% if @usermanual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usermanual.errors.count, "error") %> prohibited this usermanual from being saved:</h2>

      <ul>
      <% @usermanual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :document_name %><br>
    <%= f.text_area :document_name %>
  </div>
  <div class="field">
    <%= f.label :document_type %><br>
    <%= f.text_field :document_type %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
